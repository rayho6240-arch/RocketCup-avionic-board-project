# RocketCup Avionic Board - 專案開發進度與待辦事項

這裡記錄了成大 ISP 航電組的硬體送印狀態、硬體複用策略以及當前的開發里程碑。

---

## 目前 Git 分支節點

| 分支 | 最新 Commit | 日期 | 說明 |
|------|------------|------|------|
| `total_test`（**當前**） | （見下方本 session 變更） | 2026-06-11 | 全系統整合 + LoRa 雙鏈路遙測 + P0 飛行安全改進（依 `改進計劃.md`） |
| `EKF_test` | `0a03cf8` | 2026-06-02 | 磁力計驅動 + GPS/磁力計融入 EKF + EKF 狀態原子讀取 / ADXL 死碼清理 / Flash 寫入速率分級（FSM 7狀態、動態頂點預測、熱重啟恢復基礎上） |
| `main` | `46655ea` | 2026-05-27 | Flash ring buffer、SD 卡修正、採樣率監測 |
| `rayBranch` | `e634f13` | 已 merge | GPS / 磁力計 bring-up 實驗 |

**本 session 變更（total_test，2026-06-11，依 `改進計劃.md` P0 飛行安全優先）**：
- 整體程式碼審計 + 分級改進計畫 `改進計劃.md`（風險總覽 R1–R8 / P0–P3 路線圖 / 飛行前 checklist）
- 整合工作快照 commit（先前 session 之 LoRa 雙鏈路遙測 + telemetry + soft_i2c + tests/ + Makefile，未提交部分補提交）— `538f0ef`
- **P0-A**：FSM 抽離為純邏輯模組 `fsm.c/h`（host 可測，`tests/test_fsm.c` 27 案例黃金剖面鎖定行為）；FSM 呼叫點自 BMI088 條件塊移出改 100Hz 定時（修 R2：BMI088 斷流 FSM 停擺）；硬體動作先於 printf（修 R5：點火前 ~9ms UART 阻塞）— `6e7e08b`
- **P0-B**（修 R1 副傘永不點火單點故障）：①頂點絕對失效保護 `FSM_FAILSAFE_APOGEE_MS=15s`（BOOST/COAST 皆生效，⚠️ 飛行前必以 OpenRocket 重推 1.5×t_apogee）；②baro 原始趨勢交叉檢查（峰值回落 10m × 200ms，完全不依賴 EKF，EKF 發散時在真頂點附近開傘）；③起飛第三冗餘 baro_rel>20m（pad_ref 於 PAD 期每 30s 重零）；遙測旗標 `TELEM_FLAG_FAILSAFE 0x40`；test_fsm 擴至 35 案例（EKF 卡升/全死/卡 BOOST/baro 失效閘控）— `b902d70`
- **P1-其他**：EKF `DIVERGE>3s` 垂直通道自救重置（高度拍 baro、vz σ=10m/s 重建，不再永久降級）— `45eb3be`；test_runner.py 新斷言（[HEALTH] 寬限 5s 後恆 0、[LOOP]≤450ms、[FLASH] pool 增長）— `944a054`；repo 衛生（junk 檔/8.8MB log 清除、.gitignore 補 `*.log`/`*_autosave*`/`bom/`/測試二進位）— `ee918cc`
- **P1-LoRa 健壯化**：PD6 LORA920_BUSY 自 EXTI IT_RISING_FALLING 覆寫為純輸入（USER CODE 區，杜絕 E80 SPI 期間 ISR 風暴）；E22 AUX 逾時不再靜默 → `LoRaE22_Init` 回傳 HAL_TIMEOUT、`lora433_ok=0`、遙測任務每 10s 重試
- **P1-遙測契約**：`crc16.h` 單一實作（取代 w25qxx/telemetry 兩份重複）；封包擴至 **79 bytes**（crc 前加 `health_bits`/`sensor_bits`，故障定位不再只靠 flags 摘要位）；新增 `tests/test_telemetry.c`（44 案例：CRC 黃金向量 0x29B1、全欄位 offsetof 鎖定 —— 改佈局必紅，強迫同步解碼器）；**新增地面站解碼器 `GroundStation/telemetry_decoder.py`**（sync 掃描+CRC+seq 丟包統計，`--port`/`--file`/`--selftest`，selftest 通過）
- **P0-F**（修 R7 熱啟動二次點火）：熱啟動改 `FSM_HotStartDecide()` 純函式驗證鏈（host 已測）—— 封包 CRC、狀態 ∈ BOOST..DESCENT、tick<60s（防上次飛行殘留資料誤恢復）、|封包 baro − 當下 baro|<300m；**封包 flags bit0（副傘已點火）→ 強制恢復 DESCENT + drogue_fired 鎖存，杜絕二次點火**；移除裸跳 `EKF_calibrated=1`（EKF_Init 已自 SysFlags CRC+magic 還原校準，損毀則走 P0-C 降級鏈）；恢復時以 `EKF_HotRestartRestore` 注入封包 EKF 高度/速度/四元數；遙測 `TELEM_FLAG_HOTSTART 0x80`；test_fsm 擴至 55 案例
- **P0-E**（修 R6/R8 飛行迴圈阻塞）：①飛行態（BOOST..MAIN_DEPLOY）**禁止 Flash 同步滾動擦除**（原最壞 ~400ms 阻塞主迴圈且持 SPI3 mutex），池耗盡丟該筆 + `FlashRing_GetDropCount()` 可觀測；②PAD 期背景預擦至 **64 sectors**（≈65s 飛行量，每 1s 擦 1 個，`[FLASH] pool=N/64` 1Hz 回報，⚠️ 發射檢核表：上架後等 pool 達標）；③ADC 電池讀取逾時 10→2ms 且自 50Hz Flash 寫入路徑移至 1Hz；④飛行態抑制 10Hz CSV printf（每行 ~8.7ms UART 阻塞）；⑤`[LOOP] max_us=` 1Hz 單迴圈耗時水位（HIL 斷言飛行態 <20ms 依據）
- **P0-D**（修 R4 感測器失效零偵測）：新增 `sensor_health.h`（純函式：失流>100ms/卡死 raw 同值≥1s/範圍越界 0.5s，初始化失敗自動視同失流）；main.c 三餵入點（BMI088/ADXL375/BMP388 批次提取處）+ 100Hz 彙整 `g_sensor_fault_bits`；**消費端**：BMP388 fault → EKF 樣本 `has_baro=0`（接通 P0-C 降級鏈）、FSM `FSM_SB_BARO_FAULT`、遙測 `TELEM_FLAG_SENSOR_FAULT 0x20`、1Hz `[HEALTH] sens=/ekf=` 診斷行；新增 test_sensor_health（15 案例）
- **P0-C**（修 R3 EKF 零防護）：新增 `ekf_guard.h`（純函式）+ ekf.c 三掛載點 —— baro 創新值閘控 `max(5σ,25m)`（±10km 壞值不再直接餵入）、連續拒收 1s → `EKF_HB_BARO_DIVERGE`、P 對角夾限（pos 1e4/vel 2.5e3）、狀態理智界限（z∈[−100,2000]m、vz∈[−200,350]m/s）、NaN/Inf 掃描即重建（P 回初值/高度拍至最後接受 baro/q identity）、baro 接受逾時 500ms；`EKF_GetHealthBits()`+`EKF_GetLastUpdateTick()`（FSM 端 >300ms 視同餓死）；**FSM 降級鏈**：unhealthy 時停用 EKF 三頂點路徑（防發散值誤點火），起飛=a_z/baro（不需校準）、主傘=baro≤200m、落地=2s 視窗 baro 穩定；遙測 `TELEM_FLAG_EKF_UNHEALTHY 0x10`；新增 test_ekf_guard（26 案例）+ test_fsm [7] 降級鏈（45 案例）

**前 session 變更（EKF_test）**：
- Buzzer 開機兩聲（TIM2 CH1）+ TIM2 32-bit ARR overflow 修正 — `98a3669`
- 修正 `main.c` line 262 注解（Flash 為 SPI3 非 SPI1）+ 進度/分支節點更新 — `a988775`
- 改善項目實作 #1：D（FPU context save 確認，無需改）/ I（EKF queue drop 計數）/ N（Flash dump 改 USER_BT1 觸發）— `88c5a3a`
- 改善項目實作 #2-E：SD 卡 FSM 驅動全程記錄（取代原 10 秒上限）— `5e7cd2f`
- 改善項目實作 #2-H：氣壓高度溫度補正（hypsometric + 開機鎖定發射台 T0）— `76c55b7`
- 改善項目實作 #3：A+B 設計文件 `設計_ADXL375_EKF融合與SPI1並發.md`（含 EKF 加速度單位不一致的前置阻斷項）— `6774799`
- 改善項目實作 #4-J：GPS 驅動實作（**USART6** NMEA-0183，循環 DMA + IDLE 接收，GGA/RMC 解析，融入 Flash ring buffer 經緯度與 1Hz [GPS] 遙測）。**修正計畫書的 UART 對應錯誤**：計畫原稿誤標 GPS=USART3，實際依硬體金標準（`連線和基本硬體規格表.md`）GPS(NEO-M9N)=**USART6**(PC6/PC7)、433MHz LoRa(E22)=USART3(PD9）— `8c195cb`
- 改善項目實作 #5-L：MMC5983MA 地磁計驅動（**I2C1** 0x30，18-bit one-shot 量測 + SET/RESET 橋式偏移自校；開機 ProductID 驗證、`[MAG]` 1Hz 遙測、發射台階段每 10s 自動重校）— `ace8625`
- 改善項目實作 #6：GPS 水平位置（E/N）融入 EKF（發射台第一筆有效定位鎖定原點，等距投影換算 ENU 位移 + Joseph-form 標量量測更新，R 隨衛星數縮放；**僅飛行中融合**，發射台因 ZUPT 歸零水平狀態而捨棄）— `ddbdd3c`
- 改善項目實作 #7：地磁計航向（yaw）融入 EKF（Mahony 傾斜補償，水平磁場參考投影至 ENU-East 僅約束航向；**僅發射台階段**校正陀螺 yaw 漂移，升空後純陀螺遞推）— `0ad175d`
  - ⚠️ 三項 EKF 相關融合（#6/#7）皆需 **HIL 實機驗證**：磁力計軸向與 IMU body frame 對齊（反向則於呼叫端取負號）、`Kp_mag`/GPS 量測噪聲 R 調參、GPS 原點鎖定時機。
- 改善項目實作 #8-O：`EKF_GetState()` 原子快照（`taskENTER_CRITICAL` 封拷貝，修正 EKF_Task 高優先搶佔 defaultTask 讀取造成的撕裂讀取）— `478329a`
- 改善項目實作 #9-M（部分）：`adxl375.c` 移除死碼/錯索引的重複 raw 解析（`z_raw` 誤用 `rx_data[4]`），釐清軸向約定；**不做** sign-flip（避免與 BMI088 raw 不一致）— `3266a0b`
- 改善項目實作 #10-F：Flash ring buffer 寫入速率依 FSM 分級（INIT/PAD 20Hz、飛行 50Hz、LANDED 1Hz）；飛行率取 50Hz 而非 100Hz，因滾動擦除阻塞，全速需先做 G — `0a03cf8`
- **本 session 暫緩（需上板 / HIL 或硬體）**：🔴 A（ADXL 融入 EKF，含 g↔m/s² 單位前置修正）、B（SPI1 ISR→task 重構 + mutex）皆已備設計文件 `設計_ADXL375_EKF融合與SPI1並發.md`；🟡 G（擦除移獨立任務，需 SPI3 mutex + 時序驗證）與其前置 C（SPI3 mutex 骨架）相依，建議與 G 同批做；🟢 K（LoRa E22 遙測）需實體模組與透傳協定驗證。
- **🚨 磁力計防鎖死架構最佳化：回歸軟體 I2C + 100Hz polling (2026-06-03)**：
  * **決策**：在評估 STM32F4 晶片硬體 I2C 嚴重的 silicon bug 鎖死（BUSY 旗標 stuck、DMA 未正常釋放等）後，考量到飛行震動與馬達噪聲干擾，決定完全停用「硬體 I2C + DMA」，改回**軟體 I2C (Bit-Bang) 100Hz 連續暫存器 polling 讀取**。該架構為純軟體 fixed-bound 迴圈，具有天然免疫 Errata 及不卡死 MCU 特性，安全保障火箭飛行控制。
  * **硬體走線與 GPS 鮑率修復**：實體板線路（PB7=SCL, PB8=SDA）與 GPS (USART6 38400 bps) 均能完美配合工作。

---

## 專案重大調整與複用策略

由於**成本預算考量**，我們對航電硬體進行了極大化的複用與簡化，目前調整如下：

### 1. 首批送印板子 (共 5 張)

首批打樣送印僅選擇以下 5 張核心電路板：

1. `newpower` (位於 `BigAvionic/power layer_new`) - 主航電電源層板
2. `lora` (位於 `BigAvionic/lora`) - 主航電無線遙測通訊層板
3. `mainmcu` (位於 `BigAvionic/Second_layer_MCU`) - 主航電核心主控層板
4. `top` (位於 `BigAvionic/top_layer`) - 主航電頂層接口板
5. `Deployment board` (位於 `Deployment board/`) - 開傘部署控制板

### 2. 複用與替代策略 (Cost Optimization)

- **副航電備份板 (SmallAvionic) -> 由 `mcu layer` 代替**
  - 取消原先獨立設計的 `SmallAvionic` 系列（如 Joyce、Ray、Tank、Tony 版）。
  - 直接採用主航電四層疊板中的 `mainmcu` (MCU Layer) 單板作為備份副航電板，實現硬體完全一致性與零額外打樣費。
- **地面接收站 (GroundStation) -> 由 `BigAvionic` 整體四層疊板代替**
  - 取消原本獨立的 `GroundStation` 地面站 PCB 打樣。
  - 由於 `BigAvionic` 整體四層疊板已配備完整的 STM32 主控、LoRa 射頻收發與 USB-TTL 界面，地面接收站將直接使用一套完整組裝的 `BigAvionic` 四層疊板代替。

### 3. 預算最佳化方案 - 二合一合併板

- `Production_MCU_TOP_2_in_1` (原名 Production_MCU_LORA_2_in_1)
  - 實際上為 `top layer` (頂層板) 與 `mcu` (主控板) 合成的二合一合併板。
  - 用於作為未來量產或高度整合時的低成本備選方案（減少一張板子的打樣費用）。

---

## 實地測試里程碑 (Testing Milestones)

- **2026-06-03：電子羅盤實體線路接反與 GPS 鮑率錯誤重大定位與軟體戰術救援成功**
  - **實測發現**：實體板上 I2C1_SCL 誤接至 PB7，I2C1_SDA 誤接至 PB8，使晶片硬體 I2C 功能（PB7 限 SDA, PB8 限 SCL）受限，發送與接收訊號完全錯置；且 GPS 預設工作在 38400 bps 與軟體原配置之 115200 bps 不匹配。
  - **解決方案**：
    1. **軟體 I2C 救急**：編寫專用軟體模擬 I2C (Bit-Bang) 驅動，定義 SCL=PB7, SDA=PB8，採用 Open-Drain 開漏輸出並搭配板上 2.7kΩ 實體上拉電阻，實現在「不修改電路板」的前提下完美起死回生 MMC5983MA，實測遙測讀取高達 `ok=921, err=0`！
    2. **鮑率修正**：將 `huart6` (USART6) 鮑率修正為 38400 bps，實現 GPS 語句 `ok=60, err=0` 零校驗錯誤高速對齊！
    3. **新增頻率觀測**：在 `rate_monitor` 擴充 MMC5983MA 與 GPS 實測 Hz 統計，將 `[RATE]` 行升級回報。
  - **硬體物理優化建議**：由於頂層接口板 (Top Board) 與主控板 (MCU Board) 是透過 **JST-GH 連接線** 連接。如果日後希望恢復標準的 STM32 硬體 I2C1，**完全不需要重新打樣 PCB！只需要用細針或鑷子挑開並對調該 JST-GH 連接線接頭中的 PB7 與 PB8 兩根線針位置**，即可在硬體層面物理修復交換，並切換回原本的硬體 I2C 代碼！

- **2026-06-01：Buzzer 開機提示音實作（兩聲漸高）**
  - 實測結果：透過 TIM2 CH1 PWM（timer clock = 1 MHz，輸出腳 PA0）實現開機兩聲提示音（2 kHz → 4 kHz，各 100 ms，間隔 100 ms），於 `StartDefaultTask` 的 `Flash_DumpAll` 之後、SD 卡初始化之前播放，代表系統初始化完成。過程中發現並修正 TIM2 ARR overflow bug：TIM2 為 32-bit timer，動態縮小 ARR 時若當下 CNT 已大於新的 ARR，計數器會一路數到 `0xFFFFFFFF`（約 4295 秒）才歸零，導致 buzzer 在第三聲後完全不響。修正方法為設定 ARR/CCR1 後立即寫入 `EGR = TIM_EGR_UG` 強制 update event，使 CNT 立刻歸零並套用新週期。

- **2026-05-27：printf 重定向 USART2 (115200 baud) 實現即時串口波形監控**
  - 實測結果：在 `main.c` 新增 `_write()` syscall 覆寫，將標準 `printf` 輸出重定向至 USART2（連接 ST-LINK 虛擬串口）。主迴圈每 50ms (20 Hz) 透過 UART 輸出 BMI088 加速度、ADXL375 高 G 加速度、BMP388 氣溫/氣壓/高度等共 9 個數值欄位的 CSV 格式串流，搭配 Serial Plotter / Python 腳本即可即時繪製 IMU 波形。每 1000ms 另輸出一次各感測器實際採樣率（`[RATE]` 格式），便於自動化測試斷言分析。

- **2026-05-27：採樣率監測模組 (Rate Monitor) 整合，全感測器實測 Hz 可視化**
  - 實測結果：新增獨立模組 `rate_monitor.h`，以巨集 `SAMPLING_RATE_DECL()` 宣告全域結構體 `g_sampling_rate`，包含 BMI088 加速度計、陀螺儀、ADXL375、BMP388 各通道的 `.rate_hz` 欄位。各中斷與任務分別呼叫 `RATE_TICK_BMI088_ACC()`、`RATE_TICK_BMI088_GYRO()`、`RATE_TICK_ADXL375()`、`RATE_TICK_BMP388()` 進行累加，模組每秒自動計算並更新真實物理採樣頻率。在 IDE Watch / Live Expressions 只需加入 `g_sampling_rate` 即可一次觀察全部感測器 Hz。可透過 `rate_monitor.h` 中 `#define RATE_MONITOR_ENABLE` 的有無開關整個功能，零停機代價。

- **2026-05-27：IWDG 獨立看門狗配置完成，更新為 2.0 秒快速超時以保障飛行安全**
  - 實測結果：配置 IWDG 使用 LSI 32 kHz 振盪器，16 分頻（計數頻率 2000 Hz），載入值 4095，超時時間精準設定為 **2.05 秒**。超短的超時時間可保障在火箭飛行中如遇強干擾死機時能以最快速度重啟。通過 FreeRTOS 將 SD 卡 / FatFs 磁碟讀寫等耗時任務完全隔離在 `DataLogging_Task`，主迴圈每個 `tick`（1 ms）均呼叫 `HAL_IWDG_Refresh()` 餵狗，保障系統高穩定性不誤復位。

- **2026-05-27：W25Q128 SPI Flash 啟動自檢與寫入校驗 100% 成功**
  - 實測結果：透過 SPI3 匯流排 (SCK=PB3, MISO=PB4, MOSI=PB5) 與 CS=PA15 成功讀取 W25Q128 JEDEC ID (`0xEF4018`)，完成 4KB Sector 0 擦除、61 字節頁寫入、逐字節讀回比對全數通過。驅動程式模組化設計，包含 `w25q128.h` / `w25q128.c`，實作寫使能、忙碌超時輪詢、頁寫入、連續讀取等完整 HAL SPI 阻塞傳輸 API。由於 SPI3 為獨立匯流排，與 SPI1 (ADXL375/BMP388) 和 SPI2 (BMI088) 完全無匯流排衝突。

- **2026-05-27：SD 卡 10 秒高頻數據日誌 (HIL_10S.CSV) 寫入與無卡插拔防卡死優化成功**
  - 實測結果：實現了在 MCU 啟動後自動延遲掛載 SD 卡，並進行為期 10 秒的 100 Hz CSV 高頻遙測日誌寫入（儲存至 `HIL_10S.CSV` 檔案中，包含系統 Tick、BMI088、ADXL375、BMP388 所有感測器數據）。在寫入 10 秒後安全關閉檔案並卸載磁碟。針對無實體 SD 卡插入的 HIL 測試場景，將 `SDIO_DET_Pin` (PD3) 引腳配置由 `GPIO_NOPULL` 優化為 `GPIO_PULLUP`，徹底避免了引腳浮空導致 MCU 假性插卡檢測，使系統無卡時能優雅跳過掛載與 `f_open` 寫入，保證串口遙測輸出的絕對流暢與穩定，零卡死！

- **2026-05-27：TIM6 + TIM7 定時器獨立中斷 + 雙緩衝區 (Ping-Pong Buffer) BMI088 加速度 1600 Hz & 陀螺儀 2000 Hz 測試成功**
  - 實測結果：配置硬體定時器 TIM6 觸發 1.6 kHz 中斷（只讀取 BMI088 加速度計並塞入 16 筆的 Ping-Pong 緩衝區），配置 TIM7 觸發 2.0 kHz 中斷（只讀取 BMI088 陀螺儀並塞入 20 筆的 Ping-Pong 緩衝區）。FreeRTOS 任務每 10ms (100Hz) 提取各自批次最新數據進行遙測。因為 SPI2 匯流排專用於 BMI088，無任何中斷-任務競爭衝突，完全免除軟體互斥鎖。實測加速度計與陀螺儀物理採樣頻率精準達到官方設計之最高物理上限：**加速度計 1600.0 Hz & 陀螺儀 2000.0 Hz**！

- **2026-05-27：TIM3 定時器中斷 + 雙緩衝區 (Ping-Pong Buffer) ADXL375 3200 Hz 測試成功**
  - 實測結果：利用硬體定時器 TIM3 觸發 3.2 kHz 中斷，在中斷內直接透過 SPI1 讀取 ADXL375 原始資料（耗時僅 5.6us），並存入 32 筆資料的 Ping-Pong 雙緩衝區。FreeRTOS 任務每 10ms (100Hz) 提取批次最新數據進行遙測，上下文切換開銷降低 96.8%。實測物理採樣頻率精準達到理論最高 **3200.0 Hz**！
  - 另外開啟 BMP388 至 200 Hz ODR，並加入 SPI1 總線互斥安全鎖，在讀取 BMP388 期間屏蔽 TIM3 中斷防止衝突。將系統 LED 翻轉減緩為溫和的 0.5 秒 (1 Hz) 心跳指示。

- **2026-05-25：頂層接口板 (Top Layer) -> Arduino 整合測試成功**
  - 測試結果：成功透過 Arduino (ESP32) 讀取 I2C 磁力計 (`MMC5983MA`) 與解析 UART2 GPS (`NEO-M9N`) 的實時同步數據流。所有實體走線與通信介面工作完美。
  - 目前狀態：等待首批 `BigAvionic` 板子打樣完成後，正式與 STM32 主控板進行韌體整合測試。

---

## 開發環境設定

### 1. 快速開始 (本地 IDE 輕量化 GCC 模式)

**必要工具鏈（已安裝）**：

- `arm-none-eabi-gcc` v16.1.0（透過 Homebrew 安裝於 `/opt/homebrew/bin/`）
- `arm-none-eabi-binutils` v2.46.0

**注意**：主晶片因缺料改用 **STM32F407VGT6**（Pinout 與 STM32F405VGT6 完全一致），氣壓計改用 **BMP388**（Pinout 與 BMP390 完全一致）。

### 2. 編譯步驟

1. 確認工具鏈在 PATH 中：
   ```
   arm-none-eabi-gcc --version
   ```
2. 執行編譯（使用 4 核心並行編譯）：
   ```
   make -j4
   ```
3. 編譯成功後，會在 `Main_AV_F407/Debug/` 目錄下自動產生以下執行與燒錄檔：
   - `bigav_407.elf` (用於除錯)
   - `bigav_407.hex` / `bigav_407.bin` (用於燒錄)

### 3. 一鍵燒錄與實體單步除錯

推薦在 IDE 中安裝 **Cortex-Debug** 擴充套件，並在專案根目錄 `.vscode/launch.json` 中配置 ST-LINK 除錯器，即可在 IDE 中按下 F5 實作 ST-Link 一鍵燒錄與中斷點除錯。

---

## 環境設定檢查

| 工具 | 狀態 | 版本 | 安裝路徑 |
|------|------|------|----------|
| arm-none-eabi-gcc | 已安裝 | 16.1.0 | /opt/homebrew/bin/ |
| arm-none-eabi-binutils | 已安裝 | 2.46.0 | /opt/homebrew/bin/ |
| OpenOCD / ST-LINK | 待安裝 | - | - |

---

## 當前開發進度表 (TodoList)

進度追蹤提醒：每當您或團隊成員在進度表勾選完成項目（將 `[ ]` 改為 `[x]`）時，請務必在該項目後方標註實際完成日期（例如：`(2026-05-25 完成)`），以便精確掌控研發時程。

### 階段一：硬體設計與送印 (已完成)

- [x] 主航電四層板結構規劃 (`top`, `mainmcu`, `lora`, `newpower`) (2026-05-25 完成)
- [x] 開傘部署板完成佈局並導出 Gerber (2026-05-25 完成)
- [x] 設計二合一合併板 `Production_MCU_TOP_2_in_1` 以利成本最佳化 (2026-05-25 完成)
- [x] 完整解析 MCU、LoRa、Top、Power 原理圖 PDF，建立航電連線與基本硬體規格表 (2026-05-25 完成)
- [x] 完成 STM32CubeMX (.ioc) 核心 NVIC、DMA、FreeRTOS 與 FATFS 的硬核架構配置與交接規格書 (2026-05-26 完成)
- [x] **送印打樣**：聯絡工廠進行首批 5 張板子的製作打樣 (2026-05-25 完成)

### 階段二：樣板焊接與硬體除錯 (進行中)

- [ ] **上電短路檢測 (Safety Short-Circuit Check)**：確認各電源軌 (5V, 3.3V, VBAT) 無對地短路，防止燒毀主控與高價感測器。
- [x] 收到樣板：首批 5張核心電路樣板已製作完成並順利送回 (2026-05-25 完成)
- [ ] 樣板焊接：進行元件手工焊接與鋼網刷錫膏
- [ ] 電源軌測試：確認 5V/3.3V DCDC 降壓紋波與 LTC4412 理想二極體路徑切換正常
- [ ] 最小系統測試：確認 STM32F4 主控晶片燒錄，點亮 LED 並完成 GPIO 測試

### 階段三：軟韌體整合與射頻通訊聯調 (進行中)

- [x] **UART printf 串口重定向**：實作 `_write()` syscall 將 printf 重定向至 USART2，以 20 Hz CSV 即時輸出 IMU + 氣壓計數值，支援串口波形監控與 Python 自動化測試腳本 (2026-05-27 完成)
- [x] **採樣率監測模組 (rate_monitor.h)**：整合全感測器實測 Hz 可視化，`g_sampling_rate` 可在 IDE Watch 一次觀察所有感測器採樣率，每秒輸出一次 `[RATE]` 行 (2026-05-27 完成)
- [x] **IWDG 看門狗配置**：2.0 秒超時，提供飛行快速死機復位保護，配合 FreeRTOS 任務隔離防誤觸發，主迴圈每 1 ms 餵狗 (2026-05-27 完成)
- [x] **氣壓計高度計測試**：SPI 讀取 `BMP388` 數據，並在 200 Hz ODR 下實現 1x 過採樣穩定換算實時高度 (2026-05-27 完成)
- [x] **高頻定時器中斷與雙緩衝採樣測試 (ADXL375)**：實作 TIM3 3.2 kHz 中斷 + 雙緩衝 (Ping-Pong Buffer) 打包機制，讓 ADXL375 (High-G) 實測頻率精準達到 **3200 Hz** 理論最高極限，並加入 SPI1 總線互斥安全遮罩與將系統 LED 頻率降至 0.5s 一次 (2026-05-27 完成)
- [x] **高頻定時器中斷與雙緩衝採樣測試 (BMI088)**：實作 TIM6 1.6 kHz (加速度計) 與 TIM7 2.0 kHz (陀螺儀) 獨立中斷與雙緩衝 (Ping-Pong Buffer) 機制，實測頻率精準達到各自最高極限 **加速度計 1600 Hz & 陀螺儀 2000 Hz**，免除 SPI2 總線衝突與互斥鎖開銷 (2026-05-27 完成)
- [ ] **地磁計校準與 AHRS 姿態解算**：讀取 `MMC5983MA` 電子羅盤定位，並結合 BMI088 1.6kHz + 2.0kHz 高頻姿態數據流，實現 3D 姿態估算 (AHRS)
- [ ] **頂點偵測演算法 (Apogee Detection Algorithm)**：開發基於氣壓高度數據的卡爾曼濾波 (Kalman Filter) 算法，確保頂點判定精確且不受機艙噪聲干擾。
- [ ] **FSM 飛行狀態 Flash 備份與熱啟動自動恢復**：實作在 `init` 階段讀取 `W25Q128` Sector 0 的系統狀態旗標，在飛行中斷電重啟時自動空中熱接管 (Saved State 恢復)，並設計防重複部署安全鎖，徹底防止高G與開傘瞬間斷路引發的安全事故。
- [ ] **GPS 收星調試**：`NEO-M9N` 連續收星定位測試，驗證主動天線訊號強度
- [x] **SPI Flash 記錄測試**：實現 `W25Q128` 高速航電數據日誌 (Data Logging) 讀寫。完成 SPI3 匯流排驅動、JEDEC ID 驗證、4KB 扇區擦除、頁寫入與 100% 逐字節校驗 (2026-05-27 完成)
- [x] **Flash 環形緩衝區 (Ring Buffer) 實裝與 HIL 驗證**：在 `w25qxx.c` 實作完整環形緩衝區寫入機制。包含 52-byte packed 飛行數據封包（含 CRC-16/CCITT 校驗、magic bytes 0xAA 0x55、seq 滾動序號），開機二元搜尋恢復寫入頭（斷電後自動接續），預擦 10 個 Sector（40KB）以確保寫入區已清除，滾動擦除防止衝突。以 20 Hz 寫入速率，30 秒 HIL 測試寫入 461 筆封包（>400 門檻 ✅），Flash dump 驗證魔術字節與數據完整性 100% 通過 (2026-05-27 完成)
- [x] **SD 卡檔名自動遞增與立即掛載修復**：修正 SD 卡檔案命名邏輯，每次開機自動掃描 `HIL_001.CSV`～`HIL_999.CSV`，找到第一個不存在的序號並建立新檔，避免覆蓋上一次測試數據。同時修正 `f_mount` 由延遲掛載 (opt=0) 改為立即掛載 (opt=1)，解決首次 `f_stat` 回傳 `FR_NOT_READY`（錯誤碼 3）導致建立檔案失敗的問題 (2026-05-27 完成)
- [x] **採樣率延遲評估修正（FlashRing_Init 後重置計數器）**：`FlashRing_Init()` 預擦 10 個 Sector 阻塞約 2 秒，期間 BMP388（主迴圈讀取）無法累積計數，導致首報 Hz 嚴重低估（171 Hz 而非實測 ~196 Hz）。解法：`FlashRing_Init()` 完成後立即呼叫 `SAMPLING_RATE_INIT()` 重置所有感測器計數器，確保 `[RATE]` 報告反映穩態採樣率 (2026-05-27 完成)
- [x] **Buzzer 開機提示音**：TIM2 CH1 PWM 實作開機兩聲漸高提示音（2 kHz → 4 kHz），並修正 TIM2 32-bit ARR overflow 導致 buzzer 停響的 bug（`EGR = TIM_EGR_UG` 強制歸零 CNT）(2026-06-01 完成)
- [x] **SD 卡全程記錄（FSM 驅動）**：移除原 10 秒上限，改為全程記錄——升空至落地前 100 Hz，落地後（新增 `g_touchdown_tick` 標記，非 `STATE_LANDED` 進入點）降為 10 Hz 繼續記錄，由 USER_BT1 或落地逾時（`SD_LANDED_LOG_TIMEOUT_MS`，預設 30 分）停止並安全關檔。CSV 新增 `fsm` / `ekf_alt_cm` / `ekf_vel_cms` 欄位，每秒 `f_sync` 限制斷電遺失。移除落地 `vTaskSuspend`，使遙測 / Flash ring / 尋標蜂鳴器於落地後持續運作 (2026-06-01 完成)
- [x] **GPS 驅動實作（USART6 NMEA-0183）**：自包含 NMEA 解析器（`gps.c` / `gps.h`），解析 GGA（定位品質/衛星數/海拔）與 RMC（定位/地速/航向/有效性），XOR checksum 驗證、`ddmm.mmmm` 經緯度換算（×1e6 deg）、knots→m/s。RX 採 USART6 循環 DMA + IDLE-line（`HAL_UARTEx_ReceiveToIdle_DMA`，事件回呼以 old_pos 差分取出新位元組組句、僅設旗標），解析於主迴圈 task context 進行避免 ISR 內繁重字串處理；`HAL_UART_ErrorCallback` 處理 overrun 自動重啟 DMA RX。256-byte DMA 緩衝置於主 SRAM（0x20000150，DMA2 可存取，非 CCM）。有效定位之經緯度填入 Flash ring buffer，新增 1 Hz `[GPS]` 遙測行（fix/衛星數/經緯度/海拔/地速/stale/解析統計）。**過程中發現並修正計畫書 GPS/LoRa 的 UART 對應顛倒**（詳見修訂紀錄）(2026-06-01 完成，待硬體收星驗證)
- [x] **地磁計驅動（MMC5983MA, I2C1）**：自包含驅動 `mmc5983.c` / `mmc5983.h`，I2C1 位址 0x30，18-bit one-shot 量測（TM_M 觸發 + STATUS 輪詢 + 三軸 18-bit 組裝）；SET/RESET 雙極性量測消除橋式偏移（`offset=(set+reset)/2`，量測後留 SET 極性供快速單次讀取）；開機 ProductID（0x30）驗證、`gauss`/heading 換算（16384 LSB/Gauss）、`[MAG]` 1 Hz 遙測、發射台階段每 10 s 自動重校 (2026-06-02 完成，待硬體驗證軸向)
- [x] **GPS 水平位置（E/N）融入 EKF**：發射台第一筆有效定位鎖定原點（lat0/lon0），等距投影換算 ENU 水平位移，以 Joseph-form 標量量測更新套用於 E/N 兩軸，量測噪聲 R 隨衛星數縮放（≥8→6.25 / ≥6→25 / ≥4→100 / else 2500）；**僅飛行中融合**（發射台因 ZUPT 歸零水平狀態而捨棄），高度仍交由氣壓計 (2026-06-02 完成，待 HIL 驗證)
- [x] **地磁計航向（yaw）融入 EKF**：Mahony 傾斜補償互補修正，水平磁場參考投影至 ENU-East（`bx=√(hx²+hy²)`, `bz=hz`, by=0）使其僅約束航向；body 預測磁場 `w=Rᵀ·b`、誤差 `e=m×w` 以陀螺修正積分（Kp_mag=1.0, dt=0.1s）；**僅發射台階段**（校準後、未升空）鎖定絕對航向、抑制陀螺 yaw 漂移，升空後純陀螺遞推 (2026-06-02 完成，待 HIL 驗證軸向/增益)
- [x] **EKF 狀態跨任務原子讀取（改善項目 O）**：`EKF_GetState()` 以 `taskENTER_CRITICAL` 封住對 `EKF_x`/`EKF_q`/`EKF_accel_bias` 的拷貝，修正 EKF_Task（High）搶佔 defaultTask（Normal）讀取時的撕裂讀取（位置/速度跨輪不一致）(2026-06-02 完成)
- [x] **ADXL375 解析死碼清理（改善項目 M 部分）**：移除 `ADXL375_ReadData` 中以錯誤索引（`z_raw` 用 `rx_data[4]`）解析後又被正確碼覆蓋的死碼，補 byte-map 註解並釐清軸向約定（與 BMI088 一致：驅動層輸出感測器原生軸、反向修正集中於 EKF）(2026-06-02 完成)
- [x] **Flash 寫入速率 FSM 分級（改善項目 F）**：ring buffer 由固定 20Hz 改為 INIT/PAD 20Hz、飛行（BOOST..MAIN_DEPLOY）50Hz、LANDED 1Hz；飛行率取 50Hz 而非 100Hz，因 `FlashRing_WritePacket` 內含同步滾動 sector 擦除（最壞 ~400ms 阻塞）會卡住感測器/EKF 饋入，全速需先做 G(2026-06-02 完成)
- [ ] **LoRa 無線拉距通訊**：使用 BigAvionic 作為地面站接收端，進行公里級發射接收封包損耗測試

### 階段四：開傘安全與地面引爆測試 (待啟動)

- [ ] **引爆迴路導通檢測 (Continuity Check)**：測試 `Deployment board` 的安全監測電路，確保能透過微弱電流精確判斷火工品引信是否導通，同時不觸發引爆。
- [ ] **安全保險開關 (Arming Switch) 邏輯驗證**：驗證實體保險開關，確保地面上電前完全斷開，火箭解鎖後才允許高壓驅動。
- [ ] **火工品地面點火模擬測試 (Ground E-Match Firing Test)**：在戶外安全環境下，進行實體火工品 (E-Match) 電子點火觸發測試，確認 DCDC 開關電源不會因瞬間大電流而崩潰。

### 階段五：環境適應性與可靠度模擬測試 (待啟動)

- [ ] **真空艙氣壓高度測試 (Vacuum Chamber Altimeter Test)**：將組裝好的航電艙置於真空艙中，進行快速抽氣與放氣以模擬火箭起飛與降落，驗證高度計數據讀取與雙階段開傘觸發時機是否精確。
- [ ] **抗震與硬體抗噪測試 (Vibration & Noise Check)**：在模擬火箭發動機燃燒的震動環境下測試感測器，確認噪聲不會引發頂點誤判定，並驗證實體接線抗震可靠度。
- [ ] **高低溫電池效率測試 (Thermal & Battery Test)**：測試低溫環境下（模擬高空環境），板載鋰電池的放電效率與電壓穩定性。

### 階段六：整機整合與發射全系統模擬演練 (待啟動)

- [ ] **航電艙整機裝配 (Avionics Bay Integration)**：完成多層疊板結構物理鎖緊、天線固定、防風防強光影響的氣壓艙孔結構設計。
- [ ] **實時遙測上位機 Dashboard 聯調**：地面接收端 (`BigAvionic`) 與電腦上位機連線，實時接收封包並在 GUI 繪製即時高度、3D 姿態與地圖 GPS位置。
- [ ] **發射前倒數 SOP 演練 (Launch Countdown SOP)**：從航電艙上電、自檢 (Self-Test)、GPS 定位確認、保險上膛 (ARM)、發射倒數點火的完整流程演練，確保發射當天零失誤。

---

## 重要修訂紀錄

- **2026-06-01（改善項目實作 #1）**：依改善計畫（`context-rosy-kahan.md`）實作三項低風險項目，皆通過 STM32CubeIDE 工具鏈編譯。
  - **D — FPU context save 確認（無需修改）**：FreeRTOS 採 `ARM_CM4F` port + 編譯旗標 `-mfloat-abi=hard`（`__FPU_USED=1`，`SystemInit` 已設 CPACR）。`port.c` 於 `xPortStartScheduler` 呼叫 `vPortEnableVFP()` 並設定 `FPCCR` 的 `ASPEN|LSPEN`（硬體 lazy FPU context 保存），PendSV 依 EXC_RETURN 自動保存 S0–S31/FPSCR。`FreeRTOSConfig.h` 的 `configENABLE_FPU 0` 僅 ARMv8-M（CM23/CM33）port 會讀取，於 M4F 無作用——故 EKF 浮點運算在任務切換時本就安全。
  - **I — EKF queue drop 計數（觀測先行）**：`osMessageQueuePut` 失敗時遞增 `g_ekf_queue_drops`，於 `[RATE]` 行新增 `EKF_DROP` 欄位輸出。釐清一個計畫原稿的隱患：EKF queue 傳遞的是 **2 槽 ping-pong buffer 指標**（`g_ekf_buffers[2]`，索引以 `!idx` 切換），若僅把 queue 深度擴大到 8 而不同步擴大實體 buffer pool，會造成 stale-pointer 覆寫；且實測 `bss` 已達 ~113 KB / 128 KB SRAM，擴大為 8 buffer（+~22 KB）會溢位。故先計數觀測，確認確實有 drop 再評估「擴 pool + 改 modulo-N 索引 + 移入 CCM」。
  - **N — Flash dump 改條件觸發**：開機 `Flash_DumpAll(&hspi3)` 改為僅在按住 `USER_BT1`（上拉輸入，按下 = LOW）時執行，平時開機省下整顆 Flash 輸出的 ~3–5 秒。
  - **暫緩（需硬體在環驗證）**：🔴 A（ADXL375 融入 EKF 高 G 相位，飛行關鍵融合變更）、B（SPI1 mutex；因 ADXL375 目前於 TIM3 ISR 內讀取，mutex 不可用於 ISR，需先將讀取改為「ISR notify → task 讀取」，屬 3.2 kHz 採樣路徑重構）。建議於可上板測試的 session 進行。

- **2026-06-01（改善項目實作 #2）**：續做兩項功能完善，皆通過工具鏈編譯（無警告）。
  - **E — SD 卡 FSM 驅動全程記錄**（`5e7cd2f`）：移除原開機 10 秒上限——飛行全程 100 Hz、落地後降 10 Hz 持續記錄；新增 `g_touchdown_tick` 標記（落地是 `STATE_LANDED` 內的子條件，非進入點）；停止條件為按 `USER_BT1` 或落地逾時 `SD_LANDED_LOG_TIMEOUT_MS`（預設 30 分）後安全關檔；CSV 新增 `fsm` / `ekf_alt_cm` / `ekf_vel_cms` 三欄；每秒 `f_sync` 限制斷電遺失。移除原落地 `vTaskSuspend(NULL)` 與內嵌關檔，使遙測 / Flash ring / 尋標蜂鳴器於落地後仍持續運作。
  - **H — 氣壓高度溫度補正**（`76c55b7`）：將 `bmp388.c` 的高度換算由 ISA 定溫公式 `44330·(1-(P/P0)^0.190295)`（固定假設 15 °C）改為 hypsometric 等溫層公式 `h = (R/g)·T0·ln(P0/P)`，並於開機自檢時取 16 筆 BMP388 溫度平均、以 `BMP388_SetReferenceTemp()` 鎖定發射台 T0（範圍檢查 −40~85 °C，否則維持 288.15 K）。**關鍵安全設計**：T0 在 EKF 校準（RTOS 啟動後 3 秒平均）之前即鎖定且全程固定，使 EKF 的 launchpad 參考與飛行讀數共用同一 T0 → 相對高度於發射台恆為 0 且全程連續，校準完成瞬間無階躍，故不影響 FSM 頂點偵測。參考氣壓 `P0`（101325 Pa）在相對高度相減時完全抵銷，真正影響尺度的是 T0；於預設 288.15 K 時與原公式的壓-高靈敏度差 < 0.03%（0.08324 vs 0.08326 m/Pa），高溫環境則正確放大實際高度尺度（避免主傘觸發高度與頂點高度被低估）。開機自檢讀取 BMP388（SPI1）時同步暫關 TIM3 中斷，避免與 ADXL375 的匯流排競爭。

- **2026-06-01（改善項目實作 #3：A+B 設計文件）**：產出 `設計_ADXL375_EKF融合與SPI1並發.md`，**不含飛行碼變更**，供可上板 / HIL session 實作 🔴 A、B。文件含程式碼定位、設計、code 草圖與上板驗證清單。盤點時發現**前置阻斷項**：EKF 加速度單位不一致——BMI088 樣本為 g（`bmi088.c:162`，`×24/32768`），但 `EKF_Predict`（`ekf.c:202`）以 m/s² 減去 `GRAVITY=9.80665` 並積分；靜置由校準強制抵銷（故 ZUPT/bench 靜態正常），但**動態加速度被低估約 9.81×**。ADXL375 同為 g（`adxl375.c:108`），故須先統一單位（建議全程 m/s²，`×9.80665`）才有意義融合，此即 A 必須 HIL 驗證的根因。B 釐清「FreeRTOS mutex 不可用於 ISR」（ADXL 現於 TIM3 ISR 讀 SPI1），建議先正規化現有「暫關 TIM3 IT」臨界區為 `SPI1_Lock/Unlock()` helper（B1），DMA 驅動（B3）列長期。

- **2026-06-01（改善項目實作 #4-J：GPS 驅動）**：實作自包含 NMEA-0183 驅動 `gps.c` / `gps.h`，通過工具鏈編譯（無警告）。
  - **解析**：GGA（fix 品質 / 衛星數 / 海拔 / 大地水準面分離）與 RMC（狀態 A/V / 地速 / 航向）；XOR checksum 驗證；`ddmm.mmmm`/`dddmm.mmmm` 手動換算為 deg×1e6（int32，以 double 縮放保有效位）；knots→m/s（×0.514444）；句型比對忽略 talker ID（GP/GN/GL...）。`GPS_Update()` 於主迴圈 task context 解析（每迴圈呼叫，無資料時僅檢查旗標），`GPS_GetData()` / `GPS_IsStale()` 對外只讀。
  - **接收路徑**：採 USART6 循環 DMA（DMA2_Stream1, ch5）+ IDLE-line（`HAL_UARTEx_ReceiveToIdle_DMA`）；`HAL_UARTEx_RxEventCallback` 以 `old_pos` 差分取出新位元組餵入 line-assembly，僅做位元組搬移與設旗標（ISR-safe，不呼叫 FreeRTOS API）。256-byte DMA 緩衝經 `.map` 確認落於主 SRAM `0x20000150`（DMA2 可存取，非 CCM）。`HAL_UART_ErrorCallback` 於 overrun 清旗標並重啟 DMA RX，避免 RX 死掉。
  - **整合**：有效定位之經緯度填入 Flash ring buffer 的 `gps_lat`/`gps_lon`（原全填 0）；新增 1 Hz `[GPS]` 遙測行（正負號 + 絕對值列印避開「整數部為 0 卻為負」遺失負號的陷阱）。
  - **🔧 修正計畫書 UART 對應錯誤（飛行硬體關鍵）**：計畫 `context-rosy-kahan.md` 與初版 `gps.h` 誤將 GPS 標為 USART3。對照硬體金標準 `連線和基本硬體規格表.md`（自原理圖 100% 萃取）確認：**GPS(NEO-M9N) 實體為 USART6（PC6=TX/PC7=RX，已配置循環 DMA RX），USART3(PD9) 實為 E22-400T30S 433MHz LoRa**。原 2026-05-26 紀錄的「USART6 GPS、USART3 LoRa」才正確。若照計畫接到 USART3，GPS 將永遠收不到資料且會與 LoRa 模組搶線。已全面改為 USART6 並採其既有 DMA 配置（與 USART2 遙測 DMA-circular 同模式）。
  - **待硬體驗證**：上板接 NEO-M9N，確認 `[GPS]` 行 fix→1、衛星數遞增、經緯度合理（成大約 N22.9°, E120.2°）。

- **2026-06-02（改善項目實作 #5/#6/#7：地磁計驅動 + GPS/地磁計融入 EKF）**：實作 MMC5983MA 驅動並將 GPS 水平位置與地磁計航向融入 EKF，三項皆通過工具鏈編譯（最終 ELF text=131864，無新增警告）。
  - **#5-L 地磁計驅動（`ace8625`）**：自包含 `mmc5983.c` / `mmc5983.h`。I2C1（`hi2c1`，400 kHz；STM32F407 矽晶 AF mux 強制 PB7=SDA/PB8=SCL，文件「SCL=PB7」僅命名、HAL 自動處理，驅動與接腳標號無關），7-bit 位址 0x30。流程：`IsDeviceReady` → ProductID（0x2F 應回 0x30）→ SW reset（CTRL1 0x80）+ 15 ms → BW_400HZ → 初次 SET/RESET 橋偏自校。量測：TM_M（CTRL0 0x01）觸發 + STATUS（0x08）MEAS_M_DONE 輪詢（10 ms timeout），讀 XOUT0 起 7 byte 組裝 18-bit（`(raw0<<10)|(raw1<<2)|((raw6>>shift)&0x3)`，X/Y/Z shift=6/4/2），零點 131072（2^17）、16384 LSB/Gauss。SET/RESET 消除橋式偏移後留 SET 極性供快速單次讀取。主迴圈每 100 ms 讀一次並 `EKF_SubmitMag`，發射台階段每 10 s `MMC5983_Recalibrate()`，新增 1 Hz `[MAG]` 遙測。
  - **#6 GPS → EKF（`ddbdd3c`）**：等距投影（about-launchpad equirectangular，`dN=Δlat·DEG2RAD·R`、`dE=Δlon·DEG2RAD·R·cos(lat0)`，R=6378137 m），E/N 兩軸各做一次 Joseph-form 標量量測更新（鏡像氣壓更新路徑），R 隨衛星數縮放。`EKF_SubmitGPS` 於發射台第一筆有效定位鎖定原點（`coslat0=cos(lat0)`），之後換算 ENU 水平位移、`taskENTER_CRITICAL` 發布單筆待處理量測（多次提交僅保留最新）。**僅 `EKF_in_flight` 時套用**——發射台 ZUPT 每樣本歸零水平狀態，pad 定位無意義故捨棄；高度保留給氣壓計。
  - **#7 地磁計 → EKF（`0ad175d`）**：Mahony 傾斜補償 yaw 互補修正，完全比照 EKF **既有旋轉慣例**（R 為 body→nav，重力/上方向 = R 第三列 `[r31,r32,r33]`）。將參考磁場水平分量塌縮至 nav-East（`bx=√(hx²+hy²)`、`bz=hz`、by=0）使其**僅約束航向**；body 預測磁場 `w=Rᵀ·b`（`wx=r11·bx+r31·bz`…），誤差 `e=m×w` 當作陀螺修正積分入四元數（Kp_mag=1.0，dt=0.1 s）。**僅 `EKF_calibrated && !EKF_in_flight`（發射台階段）套用**，鎖定絕對航向並抑制重力回授無法觀測的陀螺 yaw 漂移；升空後純陀螺遞推（與既有設計一致）。
  - **⚠️ 待 HIL 實機驗證（飛行關鍵）**：(1) 磁力計三軸須與 IMU body frame 對齊——bench 測試若發現航向往反向收斂，於 `main.c` 呼叫端對相應軸取負號即可（已於 `EKF_SubmitMag` 註解標記）；(2) `Kp_mag` 與 GPS 量測噪聲 R 需依實測噪聲調參；(3) GPS 原點鎖定時機（第一筆 post-calibration fix）需確認收星穩定後才鎖。融合邏輯（pad-only mag、flight-only GPS、旋轉慣例）已於程式碼定型，調參與軸向為上板項。

- **2026-06-02（改善項目實作 #8-O / #9-M(部分) / #10-F：併發安全、死碼清理、寫入速率分級）**：三項軟體層改善，皆通過工具鏈編譯（ELF text=131864，僅餘 `ekf.c` 既有的 `EKF_PrintFloat('\r\n')` multichar 警告，非本次新增）。
  - **O — EKF 狀態原子讀取（`478329a`）**：`EKF_GetState()`（`ekf.c:483`）原以無保護方式逐欄拷貝 `EKF_x[6]` 並 `memcpy` `EKF_q`/`EKF_accel_bias`。但 EKF_Task（osPriorityHigh）持續寫入這些狀態，defaultTask（Normal）於 `[TELE]` 遙測（`main.c:1367`）、FSM 動態頂點判斷、SD 記錄（`main.c:1667`）三處呼叫讀取——High-prio 任務可在拷貝中途搶佔，使回傳的 state「位置取自舊一輪、速度取自新一輪」。以 `taskENTER_CRITICAL/taskEXIT_CRITICAL` 封住拷貝（純記憶體搬移，數十週期，對中斷延遲影響可忽略），保證一致快照。EKF_Task 內部第 768 行亦呼叫本函式（同任務），FreeRTOS 臨界區計數可巢狀，安全。**釐清**：`imu_data`/`highg_data`/`baro_data` 雖也是全域結構，但實為 defaultTask 單一任務讀寫（EKF 經 ping-pong buffer + queue 傳遞，不直接讀這些結構），故無跨任務撕裂風險，O 真正的標的是 `EKF_GetState`。
  - **M(部分) — ADXL375 解析死碼清理（`3266a0b`）**：`ADXL375_ReadData` 原先以**錯誤索引**解析一次三軸（`z_raw` 誤用 `rx_data[4]` 而非 `rx_data[5]`），緊接著又以正確索引重算覆蓋——前者為死碼且具誤導性（若日後誤刪後段正確碼即引入真 bug）。移除死碼、補 byte-map 註解。**刻意不做計畫原稿的軸向 sign-flip**：經查 BMI088（`bmi088.c:162`）同樣於驅動層輸出感測器原生軸，X/Y 反向修正集中在 EKF；故目前 Flash log 內 ADXL 與 BMI 的 raw 皆為感測器原生座標、約定本就一致，單獨對 ADXL 取負反而會破壞一致性。日後 ADXL 融入 EKF（改善項目 A）時於 EKF 饋入處比照 BMI088 取負即可，註解已標明。
  - **F — Flash 寫入速率 FSM 分級（`0a03cf8`）**：ring buffer 由固定 20Hz 改為依 `current_fsm_state`：INIT/PAD 20Hz、飛行各相位（BOOST..MAIN_DEPLOY）50Hz、LANDED 1Hz。**飛行率取 50Hz 而非計畫原稿的 100Hz** 是經分析的安全取捨：`FlashRing_WritePacket`（`w25qxx.c:527`）含「同步」滾動 sector 擦除（W25Q128 4KB sector 最壞 ~400ms、典型 ~45ms 阻塞），會卡住主迴圈的感測器批次提取與 EKF 饋入；50Hz 下擦除約每 1.56s 一次（4096B/52B≈78 筆/sector），與現行 20Hz 的 3.9s 同數量級、屬已驗證可接受範圍；100Hz 則 0.78s 一次、阻塞密度過高。要安全全速須先做 G（擦除移獨立低優先任務）。亦未在此擴充 binary 封包 EKF 欄位（會破壞既定 52-byte 格式與 CRC、開機二元搜尋恢復），EKF 估算高度/速度已在 SD CSV（改善項目 E）記錄。
  - **本批暫緩項與理由**：🔴 **A**（ADXL 融入 EKF）需先修 EKF 加速度單位不一致（g↔m/s²，見 #3 設計文件）並上板 HIL；🔴 **B**（SPI1 mutex）需把 ADXL 從 TIM3 ISR 內讀取重構為「ISR notify → task 讀取」（mutex 不可用於 ISR），屬 3.2kHz 採樣路徑改寫，須上板驗證——兩者設計已備於 `設計_ADXL375_EKF融合與SPI1並發.md`。🟡 **G**（擦除移獨立任務）會引入 erase-task 與 write-path 對 SPI3 的並發，必須先有 **C**（SPI3 mutex 骨架），且優先權/優先反轉行為須上板量測，故 C+G 同批做較妥。🟢 **K**（LoRa E22 透傳遙測）需實體模組、M0/M1 模式腳與 AUX 流控、透傳封包格式，盲寫無法驗證。以上於可上板 session 進行。

- **2026-06-01**：新增 Buzzer 開機提示音（兩聲漸高 2 kHz → 4 kHz，TIM2 CH1 PWM）。修正 TIM2 32-bit ARR overflow 導致第三聲後 buzzer 完全停響的 bug（縮小 ARR 時 CNT 若大於新 ARR 需等 `0xFFFFFFFF` 才歸零，解法為 `EGR = TIM_EGR_UG` 強制 update event 即時歸零 CNT）。同步修正 `main.c` line 262 舊注解錯誤（W25Q128 Flash 為 SPI3 而非 SPI1）。完成航電系統全面技術盤點並建立改善計畫（含 SPI mutex 保護、ADXL375 融入 EKF 高G相位、SD 卡 FSM 驅動全程記錄、Sector erase 移至獨立任務、氣壓高度溫度補正等），詳見 `context-rosy-kahan.md`。(2026-06-01 完成)

- **2026-05-27**：完成 Flash 環形緩衝區 (Ring Buffer) 完整實裝與 HIL 自動化驗證。新增 `FlashRing_Init()`（開機二元搜尋恢復寫入頭 + 預擦 10 Sector）、`FlashRing_WritePacket()`（52-byte packed 封包、CRC-16/CCITT、滾動擦除）。30 秒 HIL 測試寫入 461 筆，Flash dump 驗證 magic bytes 0xAA 0x55 與數據完整。修正 SD 卡 `f_mount` 改為立即掛載解決 FR_NOT_READY 問題；修正 SD 卡 `f_stat` 邏輯實現 HIL_001.CSV → HIL_002.CSV 自動遞增。修正 `FlashRing_Init()` 後呼叫 `SAMPLING_RATE_INIT()` 重置計數器，確保第一個 `[RATE]` 報告反映穩態採樣率而非含暖機期的低估值。

- **2026-05-27**：新增 `_write()` syscall 將 printf 重定向至 USART2（115200 baud），主迴圈以 20 Hz CSV 格式輸出所有感測器即時數值，並整合 `rate_monitor.h` 模組以每秒 `[RATE]` 行回報各感測器實測 Hz；IWDG 看門狗設定為 2.0 秒超時（提供快速死機復位保護），主迴圈每 1 ms 定期餵狗。(2026-05-27 完成)
- **2026-05-27**：完成 W25Q128 SPI Flash 驅動開發與硬體迴路驗證。實作模組化 HAL SPI 驅動 (`w25q128.h`/`w25q128.c`)，包含 JEDEC ID 讀取、Power-Down 釋放、4KB Sector Erase、Page Program (≤256B)、連續讀取，以及帶超時保護的 WaitBusy 忙碌輪詢。首次嘗試使用 SPI1 匯流排失敗（JEDEC ID 回傳 `0x000000`），經用戶更正後確認 W25Q128 掛載於 **SPI3** (SCK=PB3, MISO=PB4, MOSI=PB5, CS=PA15)，切換後立即成功讀取 JEDEC ID `0xEF4018`，寫入測試字串並完成 100% 逐字節校驗。(2026-05-27 完成)
- **2026-05-27**：實作 BMI088 加速度計與陀螺儀獨立頻率採樣。TIM6 1600 Hz 中斷讀取加速度計（搭配 16 筆資料雙緩衝），TIM7 2000 Hz 中斷讀取陀螺儀（搭配 20 筆資料雙緩衝）。全面移除了 SPI2 在任務層的呼叫，由兩個中斷完全獨佔，提供高實時與零匯流排鎖定衝突的雙定時器高性能架構，實測採樣率分別高精準鎖定在 1600 Hz 與 2000 Hz。(2026-05-27 完成)
- **2026-05-27**：完成高頻定時器中斷 (Timer ISR) + 雙緩衝 (Ping-Pong Buffer) 的飛控級採樣架構優化。將 ADXL375 設定為 3200Hz ODR 並在中斷中直接讀取，實測採樣率完美達到 3200.0Hz 理論最高極限。降低 RTOS 切換開銷 96.8%（3200Hz 降至 100Hz）。解決 SPI1 總線在 TIM3 中斷與 FreeRTOS (BMP388 @ 200Hz) 之間的衝突問題，實作中斷屏蔽鎖。優化狀態反饋，將 system LED 擺動減慢為 0.5 秒 (1 Hz) 一次溫和視覺心跳。(2026-05-27 完成)
- **2026-05-26**：完成主航電 STM32F4 飛控底層配置。完成 STM32CubeMX (`.ioc`) 最終配置與硬體架構交接規格書。確立 NVIC 中斷優先級防禦線（FreeRTOS SYSCALL 優先級為 5），妥善配置 DMA 搬運通道（SDIO、USART2 雙機同步、USART6 GPS、USART3 LoRa）與 FreeRTOS (CMSIS_V2) + FATFS 中間件整合，為系統奠定堅實的實時多任務安全底座。(2026-05-26 完成)
- **2026-05-25**：首批五張樣板順利到貨。包含 `newpower`、`lora`、`mainmcu`、`top`、`Deployment board` 共五張核心電路樣板製作打樣圓滿完成，板子已順利運抵實驗室，硬體送印階段正式宣告 100% 達成。(2026-05-25 完成)
- **2026-05-25**：硬體規格與引腳連線完美解析。成功完成四層原理圖 PDF 檔案的 100% 完整解析，萃取出 STM32F407VGTx 的 100 支物理引腳實體 PCB 連線（Net Label），並建立繁體中文 `連線和基本硬體規格表.md` 黃金參考手冊。主晶片因缺料改用 STM32F407VGT6（Pinout 與 F405 完全相容），氣壓計因缺料改用 BMP388（Pinout 與 BMP390 完全相容）。(2026-05-25 完成)
- **2026-05-25**：完善長期開發里程碑。新增了極具航空航天工程標準的「地面安全與引爆測試」、「真空艙氣壓高度仿真測試」、「整機抗震驗證」與「發射前 SOP 演練」等關鍵里程碑，建構完整的航電全生命週期開發路線。
- **2026-05-25**：硬體測試里程碑。頂層接口板 (Top Layer) 成功與 Arduino 整合測試，順利實現磁力計與 GPS 的非阻塞式雙重數據同步。目前正等待發廠打樣，並進入 STM32 韌體整合準備。
- **2026-05-25**：專案架構重大重構。確立 5 板送印策略；副航電板與地面站分別改由 `mcu layer` 與 `BigAvionic 疊板` 代替。將 `Production_MCU_LORA_2_in_1` 二合一板正名為 `Production_MCU_TOP_2_in_1`，完美反映硬體架構（Top + MCU）。
